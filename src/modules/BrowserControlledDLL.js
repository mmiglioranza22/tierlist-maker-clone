import Node from './Node.js'
import DoublyLinkedList from './DoublyLinkedList.js'

class BrowserControlledDLL extends DoublyLinkedList {
  constructor(name) {
    super(name)
  }
}

export default BrowserControlledDLL