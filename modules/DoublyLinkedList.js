import Node from './Node.js'
class DoublyLinkedList {
   constructor(name){
       this.name = `${name}-tier`
       this.head = null
       this.tail = null
       this._length = 0
   } 

   prepend(value) {
       
   }

   append(value){

   }

   insert(value) {

   }

   remove() {

   }
    
}
export default DoublyLinkedList